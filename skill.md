# Fix Loop Skill

## Goal
Fix the multiply bug in `src/calc.js` so `multiply(4, 3)` returns `12`.

## Fix steps
1. Open `src/calc.js`.
2. Find the `multiply` function.
3. Change the wrong operator to `*`.
4. Do not touch `add` or `subtract`.
5. Run `npm test` and confirm it passes.

## Done when
`npm test` exits 0 and the reviewer replies PASS.
